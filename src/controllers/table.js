export const join = data => {
    console.log(`Joining table data: ${data}`);
    console.log(`Joining table this: ${this}`);
}

export const leave = data => {
    console.log(`Leaving table data: ${data}`);
    console.log(`Leaving table this: ${this}`);
}