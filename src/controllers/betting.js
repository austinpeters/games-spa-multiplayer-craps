export const add = data => {
    console.log(`Adding bet data: ${data}`);
    console.log(`Adding bet this: ${this}`);
}

export const remove = data => {
    console.log(`Removing bet data: ${data}`);
    console.log(`Removing bet this: ${this}`);
}